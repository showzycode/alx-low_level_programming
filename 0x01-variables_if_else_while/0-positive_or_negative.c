#include <stdlib.h>
#include <stdio.h>
#include <stdio.h>
/**
 *main - Entry point
 *Return always 0
 */

int main(void)
{
if (n > 0)
printf("%d is a positive\n", n);

else if (n == 0)}
printf("%d is a zero\n", n);
else

printf("%d is a nagative number\n", n);
return (0);
}
