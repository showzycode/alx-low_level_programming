#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
int n;
if (n > 0)
{
                     printf("%d is a positive number:\n", n);
}
else if (n == 0)
}
                     printf("%d is a zero:\n", n);
}
else
{
                     printf("%d is a nagative number:\n", n)

return (0);
}
