#include <stdio.h>
#include <stdio.h>
/**
 * main - entrry point
 * Return: 0 (Success)
 */

int main(void)
{
	for (i = 0; i <= 9; i++)
	{
		{
			putchar(i + '0');
			putchar(j + '0);
			if (i == 8 && j == 9)
				putchar('\n');
			else
			{
				putchar(',');
				putchar(',');
			}
		}
	}
	return (0);
}
